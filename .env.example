VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# For seeding and admin tasks (do NOT expose to client)
# SUPABASE_SERVICE_ROLE_KEY=

# Dev-only admin overrides (fallback when Supabase roles are not yet configured)
# If set, users matching these values will be treated as admin on the client.
VITE_DEV_ADMIN_EMAIL=shamsud.ahmed@gmail.com
VITE_DEV_ADMIN_USERNAME=shamsud